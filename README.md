# ai-crawler-index
A read-only, keyless reference dataset describing AI/web crawlers — including robots.txt tokens, user-agent substrings, operator documentation, robots.txt compliance, verification methods, and mirrored operator-published IP ranges normalized into one schema with provenance and SHA-256 checksums. Cov
